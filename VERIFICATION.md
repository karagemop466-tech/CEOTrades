# Line-by-line verification

2026-08-30 · 15 paper positions · Yahoo query1 opens/closes

- Entry rule: first session **after** Form 4 `fd` · failures **0**
- Arithmetic (`shares=10000/entry`, `mtm=shares*last`) · failures **0**
- Price source: `yahoo:query1` on all 15
- Split-adjustment flags: VEEE (gap > 30×), DBGI (post-entry price jump)

Full table with SEC + Yahoo links: `PAPER_LINE_REVIEW.md`.
